# SAGE - Minimal Requirements for Core Functionality
# Only includes dependencies for current working features

# Core dependencies (required for current functionality)
pyyaml>=6.0.1
psutil>=5.9.0
aiofiles>=23.0.0
click>=8.1.0

# Database (required for cache and config)
tinydb>=4.8.0

# Development and testing
asyncio-mqtt>=0.16.0

# Basic utilities
python-dateutil>=2.8.0
cachetools>=5.3.0

# Plugin manager advanced features
watchdog>=3.0.0